server:
  port: 8080

spring:
  application:
    name: aws-lab-backend

  # datasource:
  #   url: "jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true"
  #   username: ${DB_USERNAME}
  #   password: ${DB_PASSWORD}
  #   driver-class-name: com.mysql.cj.jdbc.Driver

  # jpa:
  #   hibernate:
  #     ddl-auto: update
  #   show-sql: true
  #   properties:
  #     hibernate:
  #       format_sql: true
  cloud:
  aws:
    region: ap-southeast-2   # deploy.yml 리전과 맞추세요
    s3:
      bucket: ${S3_BUCKET_NAME:awslab2026-bucket-test}